import { connect } from 'redux';
import NewServer from './new_server';

const mapStateToProps = state => ({

});

const mapDispatchToProps = dispatch => ({
  
});

export default connect(mapStateToProps, mapDispatchToProps)(NewServer);
